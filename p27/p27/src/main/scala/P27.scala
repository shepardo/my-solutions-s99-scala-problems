package com.shepardo.p99

import scala.collection.mutable.ListBuffer
import scala.util.Random

class P27[T] {

  def group3(l: List[T]) : List[List[List[T]]] = {
    // TODO: How to create default values for generic types?
    List(List(List()))
  }
}